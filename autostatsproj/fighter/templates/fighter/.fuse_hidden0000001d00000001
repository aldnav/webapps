{% load staticfiles %}
<!DOCTYPE html>
<html>
<head>
    <title>Fighter</title>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="{% static "css/bootstrap.min.css" %}" rel="stylesheet">
    <style type="text/css">
    .fighter-prev {
        display: block;
        height: 200px;
        background-color: black;
        margin-top: 2em;
        position: relative;
    }
    .fighter-prev img {
        max-width: 100%;
        max-height: 100%;
        z-index: 0;
    }
    .fighter-prev a {
        z-index: 100;
    }
    </style>
</head>
<body>
    <div class="container">
    {% if all_fighters %}
        <div class="row clearfix">
        {% for fighter in all_fighters %}
            <div class="col-md-4 column fighter-prev">
                {% if fighter.img_url %}
                    <img src="{{fighter.img_url}}" alt="{{fighter.name}}'s photo">
                {% else %}
                    <img src="{% static "img/FighterHeadShotBlank.gif" %}" alt="{{fighter.name}}'s photo">
                {% endif %}
                <a href="{{fighter.get_absolute_url}}">{{fighter.name}}</a>
            </div>
            {% if forloop.counter|divisibleby:3 %}
        </div>
        <div class="row clearfix">
            {% endif %}
        {% endfor %}
        </div>
    {% else %}
        <p>No fighters.</p>
    {% endif %}
    <div class="row clearfix">
        <div class="col-md-12">
            <form action="scrape/" method="post">{% csrf_token %}
                <label for="fm_url">Fightmatrix url: </label>
                <input id="fm_url" type="url" name="fm_url">
                <label for="mma_url">MixedMartialArts url: </label>
                <input id="mma_url" type="url" name="mma_url">
                <input type="submit" name="submit" value="Add fighter">
            </form>
        </div>
    </div>
    <script src="{% static "js/jquery.min.js"%}"></script>
    <script src=" {% static "js/bootstrap.min.js"%}"></script>
</body>
</html>